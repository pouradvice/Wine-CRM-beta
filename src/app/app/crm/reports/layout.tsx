// src/app/app/crm/reports/layout.tsx
// Pass-through layout — sub-routes redirect to /reports.
export default function ReportsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
